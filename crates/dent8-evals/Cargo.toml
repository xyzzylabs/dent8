[package]
name = "dent8-evals"
description = "Adversarial evaluation corpus for the dent8 memory firewall: attack scenarios against the real firewall, a recency-only baseline, and modeled Mem0/Zep integrity comparison."
edition.workspace = true
license.workspace = true
readme.workspace = true
keywords = ["benchmark", "adversarial", "agent", "memory", "security"]
categories = ["development-tools::testing"]
repository.workspace = true
rust-version.workspace = true
version.workspace = true

[lib]
name = "dent8_evals"
path = "src/lib.rs"

[dependencies]
dent8-core = { path = "../dent8-core", version = "0.8.0" }
dent8-store = { path = "../dent8-store", version = "0.8.0" }

[lints]
workspace = true
